# Markdown Test Notes

This file covers common Markdown reading support.

## Summary

- Headings and paragraphs
- **Bold text** and *italic text*
- `inline code`
- Links such as [Apple](https://www.apple.com)
- Local image handling when packaged with related assets

## Checklist

1. Open this file from Files.
2. Confirm the heading hierarchy is clear.
3. Confirm lists are readable.
4. Confirm code blocks use a distinct style.

> Safe Preview should keep user-selected documents local by default.

```swift
let supportedFiles = ["html", "md", "zip"]
print(supportedFiles.joined(separator: ", "))
```
