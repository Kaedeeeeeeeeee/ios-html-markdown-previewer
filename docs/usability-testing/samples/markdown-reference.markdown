# Markdown Extension Reference

This file verifies that the app appears for and imports the `.markdown` extension.

## Expected Result

- The file opens in the native Markdown preview.
- Headings, paragraphs, lists, emphasis, and inline code render correctly.
- The imported file appears in Recent files.

> Use this sample when filling the `.markdown` column in the physical-device validation matrix.

